apiVersion: v1
kind: Secret
metadata:
  name: meals-secrets
  namespace: homelab
type: Opaque
stringData:
  # Replace with your actual API key
  ANTHROPIC_API_KEY: "sk-ant-REPLACE-WITH-YOUR-KEY"
  # Optional: Add if using OpenAI
  # OPENAI_API_KEY: "sk-REPLACE-WITH-YOUR-KEY"
